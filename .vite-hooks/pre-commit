# Regenerate the design skill when the source tips change (ported from lefthook).
if git diff --cached --name-only --diff-filter=ACMR |
  grep -qx 'packages/devil-ui-docs/src/components/skill/design-tips.tsx'; then
  pnpm --filter @hellwrk/devil-ui-docs generate-design-skill
  git add packages/devil-ui-docs/public/skill.md skills/kumo-design/SKILL.md
fi
vp staged
