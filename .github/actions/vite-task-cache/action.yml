name: "Vite Task cache"
description: "Restore and save the Vite Task cache."

runs:
  using: "composite"
  steps:
    - uses: actions/cache@v6
      with:
        path: node_modules/.vite/task-cache
        # The per-run key never pre-exists, so every successful job saves.
        # Prefer this job's outputs, then main's complete warmed task graph.
        # v2 discards fingerprints collected before explicit build inputs.
        key: vite-task-v2-${{ runner.os }}-${{ runner.arch }}-${{ github.job }}-${{ github.run_id }}-${{ github.run_attempt }}
        restore-keys: |
          vite-task-v2-${{ runner.os }}-${{ runner.arch }}-${{ github.job }}-
          vite-task-v2-${{ runner.os }}-${{ runner.arch }}-warm-
          vite-task-v2-${{ runner.os }}-${{ runner.arch }}-
