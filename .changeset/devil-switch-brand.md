---
"@hellwrk/devil-ui": patch
---

Switch checked track now uses the `devil-brand` semantic token instead of hardcoded blue, so it follows the theme (red in the default theme) and adapts to light/dark mode automatically.
